import { Icon } from '@chakra-ui/react';
import { MdPerson, MdWork, MdSchool } from 'react-icons/md';

export const PersonIcon = () => <Icon as={MdPerson} />;
export const WorkIcon = () => <Icon as={MdWork} />;
export const SchoolIcon = () => <Icon as={MdSchool} />;
